#!/bin/bash -eu
#
# Handle a Radarr instance event as a Radarr Custom Script.


set -o pipefail
PS4='${LINENO}+'
test -z "${DEBUG:-}" || set -x


main() {
    servarr_name=Radarr
    export HOME=~hotio
    exec prunerr handle "${servarr_name}" "${@}"
}


main "$@" 1>&2
