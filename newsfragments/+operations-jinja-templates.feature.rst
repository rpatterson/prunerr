:Operations: Switch to `Jinja templates
             <https://jinja.palletsprojects.com/en/latest/templates/>`_ in the operation
             configurations for extracting values from download items. Also support
             dynamic comparison values for ``equals``, ``minimum``, ``maximum`` using
             Jinja templates.
